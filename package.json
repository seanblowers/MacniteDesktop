{
  "name": "macnite-desktop",
  "private": true,
  "version": "0.1.1",
  "description": "Macnite Desktop — a Ninite-style installer for Mac apps, built with Tauri.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "release": "bash -c 'set -a; [ -f .env ] && . ./.env; set +a; tauri build'",
    "icon": "tauri icon ./dist/logo.svg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
